# Use Filters in Retrievers

The aforementioned experiments assumes the search items contain unstructured text only. This assumption may not hold in real world search applications. Considering product search for example, each product not only consists of the product description (unstructured text), it also contains structured data such as product category and pricing.